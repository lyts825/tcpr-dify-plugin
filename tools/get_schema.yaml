identity:
  name: get_schema
  author: lyts825
  label:
    en_US: Get Schema
    zh_Hans: 获取 Schema
description:
  human:
    en_US: Return the production field schema, units, and active generation capabilities.
    zh_Hans: 返回生产字段 Schema、单位和当前 generation 索引能力。
  llm: Inspect the schema before constructing a typed TCPR query.
parameters: []
output_schema:
  type: object
  properties:
    message: {type: string}
    status: {type: string}
    schema_version: {type: string}
    source: {type: object}
    fields: {type: object}
    index_capabilities: {type: array}
extra:
  python:
    source: tools/get_schema.py
